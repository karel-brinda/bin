#!/usr/bin/env bash

set -euo pipefail

if [[ $# -ne 1 ]]; then
	printf 'convert a CSV file to TSV\nusage: %s in.csv\n' "${0##*/}" >&2
	exit 1
fi

awk -F ',' 'BEGIN { OFS = "\t" } { $1 = $1; print }' "$1"
