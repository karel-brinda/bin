#!/usr/bin/env bash

set -euo pipefail

if [[ $# -gt 1 ]]; then
	printf 'usage: %s [name]\n' "${0##*/}" >&2
	exit 1
fi

BASEDIR=$(dirname "$0")
if [[ $# -eq 0 ]]; then
	DESTINATION=a.Rproj
else
	DESTINATION="$1.Rproj"
fi

cp "$BASEDIR/samples/sample.Rproj" "$DESTINATION"
