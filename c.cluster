#! /usr/bin/env bash

BASEDIR=$(dirname "$0")
cp "$BASEDIR/samples/sample.cluster.snakefile"  Snakefile
cp "$BASEDIR/samples/sample.cluster.makefile"  Makefile
cp "$BASEDIR/samples/sample.cluster.json"  cluster.json

